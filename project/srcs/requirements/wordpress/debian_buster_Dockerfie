FROM debian:buster

EXPOSE 9000

RUN apt update && apt upgrade -y && apt install -y php-fpm php-mysql curl mariadb-client

COPY ./requirements/wordpress/tools/setup_wp.sh .

RUN chmod +x setup_wp.sh

CMD ["./setup_wp.sh"]
